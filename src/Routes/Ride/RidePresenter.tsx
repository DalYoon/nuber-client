import React from "react";
import styled from "../../typed-components";

const Container = styled.div``;

const RidePresenter = () => (
  <Container>
    <span>This is Ride</span>
  </Container>
);

export default RidePresenter;
